const constructorId = (konstruktor) => konstruktor.konstruktorId
const url = (konstruktor) => konstruktor.url
const name = (konstruktor) => konstruktor.name
const nationality = (konstruktor) => konstruktor.nationality

export default {
  Konstruktor: {
    constructorId,
    url,
    name,
    nationality,
  },
}
